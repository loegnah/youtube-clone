extends ../base

block content
    if errorMessage
      span=errorMessage
    form(method="POST")
        input(name="oldPassword", placeholder="Old password", type="password")
        input(name="newPassword", placeholder="New password", type="password")
        input(name="newPasswordConfirmed", placeholder="New password Confirmed", type="password")
        input(type="submit", value="submit")
